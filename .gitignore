# deps
node_modules
server/node_modules

# env
.env*.local
.env.local

# os
.DS_Store
*.tsbuildinfo

# demo repos (server-side, gitignored)
demo-repos/

# ─── React Native ───
# metro / haste
.metro-health-check*
metro.config.js.bak
# bundles
*.jsbundle
# android (regenerated by `npx @react-native-community/cli init`)
android/.gradle
android/.idea
android/app/build
android/build
android/local.properties
*.keystore
!debug.keystore
# ios
ios/Pods
ios/build
ios/*.xcworkspace/xcuserdata
*.hmap
# server (Next.js)
server/.next
server/out
