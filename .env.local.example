# Copy to .env.local and fill in.
#
# The server-side Claude Agent SDK loop reads this on every /api/agent/run
# request. Without it the route returns a clean error event to the client.
ANTHROPIC_API_KEY=

# Optional: override the default model (recommended: claude-opus-4-7).
# CLAUDE_AGENT_MODEL=claude-opus-4-7
