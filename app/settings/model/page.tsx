import { SettingsModel } from '@/components/screens/SettingsModel';
import { ScreenStage } from '@/components/ScreenStage';

export default function ModelPage() {
  return (
    <ScreenStage devNav>
      <SettingsModel />
    </ScreenStage>
  );
}
