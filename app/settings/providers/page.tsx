import { SettingsProviders } from '@/components/screens/SettingsProviders';
import { ScreenStage } from '@/components/ScreenStage';

export default function ProvidersPage() {
  return (
    <ScreenStage devNav>
      <SettingsProviders />
    </ScreenStage>
  );
}
